-- Migration 085 has been intentionally left empty because jam session tables are now
-- managed by migration 20241010120000_enable_busking_core.sql. This prevents legacy
-- definitions from conflicting with the lightweight schema introduced there.
