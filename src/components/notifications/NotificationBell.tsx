import { InboxButton } from "@/components/inbox/InboxButton";

// Kept as a compatibility export for older layout components. The separate
// notification popover has been retired; all game notifications now surface
// through the unified Inbox.
export const NotificationBell = InboxButton;
