import type { BandAgreementTerms, RevenueCategory } from './types';
import { REVENUE_CATEGORIES } from './services';

export interface AgreementTemplate { id: string; name: string; version: number; description: string; terms: BandAgreementTerms; warnings: string[]; }
const allEqual = (from: string, memberIds: string[], reserve = 0) => REVENUE_CATEGORIES.map((category: RevenueCategory) => ({ category, ruleType: 'percentage_split' as const, appliesFrom: from, unallocatedPercentage: 0, recipients: [...memberIds.map(id => ({ recipientId: id, recipientType: 'player' as const, percentage: (100 - reserve) / Math.max(1, memberIds.length) })), ...(reserve ? [{ recipientId: 'band-reserve', recipientType: 'band_reserve' as const, percentage: reserve }] : [])] }));
export function createEqualShareTemplate(from: string, memberIds: string[], reserve = 10): AgreementTemplate { return { id: 'equal-share-band', name: 'Equal-share band', version: 1, description: 'All listed members share future net band income equally after an explicit band reserve.', warnings: ['Changing the template never changes active agreements silently.'], terms: { managementResponsibilities: [], expectations: { rehearsals: 'Attend scheduled rehearsals or give notice.', gigs: 'Keep accepted gig commitments.', touring: 'Touring requires separate confirmation.', availability: 'Keep availability current.' }, revenueShares: allEqual(from, memberIds, reserve), expenseRules: ['Approved band expenses are deducted before distribution.'], fixedFees: [], creativeOwnership: { songwritingDefault: 'Song-level credits control songwriting ownership; band terms only provide defaults.', recordingDefault: 'Recording credits and master-owner records survive departure.', releaseIncomeDefault: 'Release-specific terms override band defaults.', survivesDeparture: ['songwriting ownership', 'recording credit', 'earned unpaid revenue', 'historical audit'] }, noticePeriodDays: 7, departureRules: ['Submit notice before leaving where possible.', 'Earned income and creative credits survive departure.'], removalRules: ['Removal must preserve earned income and credits.'], disputeProcess: 'Open an in-game dispute for affected unsettled items; no automatic winner is chosen.', effectiveDates: { from }, amendmentRules: ['Material changes require a new version and affected-party acceptance.'] } }; }
