import { useState } from 'react';import { toast } from 'sonner';import { Button } from '@/components/ui/button';import { mapBookingError,type BookingSide } from '../bookingTypes';import { useFestivalContractActions } from '../hooks';import type { FestivalContractRecord } from '../domainTypes';import { useStableMutationIdempotencyKey } from '../useStableMutationIdempotencyKey';
export function ContractSignaturePanel({ contract, authoritySide }: {contract:FestivalContractRecord;authoritySide?:BookingSide}){const[ack,setAck]=useState(false);const{signContract}=useFestivalContractActions(contract.id);const version=contract.current_version??1;const signed=(contract.festival_contract_signatures??[]).some(s=>s.signing_side===authoritySide&&s.contract_version===version);const idem=useStableMutationIdempotencyKey('sign-contract',contract.id,String(version));if(!authoritySide)return <p className="text-sm text-muted-foreground">Server authority does not allow signing from this session.</p>;return <div className="space-y-2"><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={ack} onChange={e=>setAck(e.target.checked)}/> I reviewed immutable version {version} and acknowledge the terms.</label><Button size="sm" disabled={!ack||signed||signContract.isPending} onClick={()=>signContract.mutate({contractId:contract.id,expectedVersion:version,signingSide:authoritySide,acknowledgement:{checked:true},idempotencyKey:idem.idempotencyKey},{onSuccess:()=>{idem.markSucceeded();toast.success('Signature recorded')},onError:e=>toast.error(mapBookingError(e).message)})}>{signed?'Already signed':`Sign as authorised ${authoritySide}`}</Button></div>}
