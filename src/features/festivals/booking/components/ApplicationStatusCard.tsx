import { useState } from 'react';
import { formatDistanceToNow } from 'date-fns';
import { Badge } from '@/components/ui/badge';import { Button } from '@/components/ui/button';import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from '@/components/ui/dialog';import { Textarea } from '@/components/ui/textarea';
import type { FestivalApplicationStatus } from '../bookingTypes';import type { FestivalApplicationRecord } from '../domainTypes';
const statusCopy: Record<FestivalApplicationStatus,string>={draft:'Draft application not yet submitted.',submitted:'Submitted and waiting for organiser review.',under_review:'The organiser is reviewing your application.',shortlisted:'Shortlisted for a booking offer.',waitlisted:'Waitlisted in case a slot opens.',offer_pending:'An offer is pending.',rejected:'Not selected for this edition.',withdrawn:'Withdrawn by your band.',expired:'The application window has expired.',converted_to_contract:'Converted into a contract.'};
export function ApplicationStatusCard({ application, onWithdraw }: { application: FestivalApplicationRecord; onWithdraw?: (reason: string) => void }){const[confirm,setConfirm]=useState(false);const[reason,setReason]=useState('');const updated=application.updated_at??application.created_at;const canWithdraw=['submitted','under_review','shortlisted','waitlisted','offer_pending'].includes(application.status);return <Card><CardHeader><div className="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><CardTitle className="text-base">{application.edition_title??application.edition_id}</CardTitle><Badge>{application.status.replaceAll('_',' ')}</Badge></div><CardDescription>{statusCopy[application.status]}</CardDescription></CardHeader><CardContent className="space-y-3"><p className="text-sm text-muted-foreground">Updated {updated?formatDistanceToNow(new Date(updated),{addSuffix:true}):'recently'}.</p>{application.reason?<p className="text-sm">Reason: {application.reason}</p>:null}{canWithdraw&&onWithdraw?<Button variant="outline" size="sm" onClick={()=>setConfirm(true)}>Withdraw application</Button>:null}<Dialog open={confirm} onOpenChange={setConfirm}><DialogContent><DialogHeader><DialogTitle>Withdraw application?</DialogTitle><DialogDescription>{application.status!=='submitted'?'Review has already begun. The organiser will see this application as withdrawn.':'This removes your band from consideration.'}</DialogDescription></DialogHeader><Textarea aria-label="Withdrawal reason" value={reason} onChange={(e)=>setReason(e.target.value)} placeholder="Optional reason"/><DialogFooter><Button variant="outline" onClick={()=>setConfirm(false)}>Cancel</Button><Button onClick={()=>{onWithdraw?.(reason);setConfirm(false);}}>Withdraw</Button></DialogFooter></DialogContent></Dialog></CardContent></Card>}
