import {describe,expect,it} from "vitest";import {FESTIVAL_UPGRADE_KEYS} from "./types";describe("Festival upgrade catalogue contract",()=>{it("contains exactly the stable eleven keys",()=>{expect(FESTIVAL_UPGRADE_KEYS).toHaveLength(11);expect(new Set(FESTIVAL_UPGRADE_KEYS).size).toBe(11);expect(FESTIVAL_UPGRADE_KEYS[0]).toBe("site_infrastructure");expect(FESTIVAL_UPGRADE_KEYS[10]).toBe("sustainability_technology")})});
