import {describe,expect,it} from "vitest";
import {formatFestivalMoney,parseFestivalSettlement,parseFestivalSettlementLine,parseFestivalSettlementSummary} from "../domain/festivalSettlement";
const id="11111111-1111-4111-8111-111111111111";
describe("Festival settlement contracts",()=>{it("formats canonical minor units with en-GB currency",()=>expect(formatFestivalMoney(123456,"GBP")).toBe("£1,234.56"));it("parses permissions without casting an RPC payload",()=>expect(parseFestivalSettlement({id,runtimeSessionId:id,festivalCompanyId:id,status:"partially_settled",currencyCode:"GBP",version:2,formulaVersion:"v1",reviewSnapshotId:id,finalSnapshotId:null,permissions:{canPrepare:false,canExecute:true,canRetry:true,canDispute:false,canReadFullSummary:true}}).status).toBe("partially_settled"));it("rejects unsafe money numbers",()=>expect(()=>parseFestivalSettlementLine({id,settlementId:id,lineType:"refund",sourceType:"refund",sourceId:id,recipientType:"player",recipientId:id,payerType:"company",payerId:id,grossAmountMinor:Number.MAX_VALUE,taxAmountMinor:0,feeAmountMinor:0,netAmountMinor:1,currencyCode:"GBP",status:"outstanding",priority:10,formulaVersion:"v1",calculationMetadata:{}})).toThrow("malformed_festival_settlement"));it("keeps gross, cash and outstanding values separate",()=>{const base={ticketRevenueMinor:100,vendorRevenueMinor:0,festivalMerchRevenueMinor:0,artistMerchGrossMinor:0,sponsorshipIncomeMinor:0,totalRevenueMinor:100,artistCostsMinor:0,staffCostsMinor:0,supplierCostsMinor:0,refundsMinor:0,taxLiabilitiesMinor:20,platformFeesMinor:0,otherCostsMinor:0,netProfitLossMinor:100,cashReceivedMinor:100,cashPaidMinor:0,outstandingReceivablesMinor:0,outstandingPayablesMinor:20,nonCashContributionsMinor:0,currencyCode:"GBP"};expect(parseFestivalSettlementSummary(base).outstandingPayablesMinor).toBe(20)})});
