import {describe,expect,it} from "vitest";
import {parseFestivalArtistActionResult,parseFestivalArtistCandidates,parseFestivalArtistOpportunities} from "../domain/festivalArtistWorkflows";
const id="11111111-1111-4111-8111-111111111111";
describe("Festival artist workflow contracts",()=>{it("parses authority-safe opportunities",()=>expect(parseFestivalArtistOpportunities({openApplications:[{windowId:id,festivalCompanyId:id,name:"Open",opensAt:"2029-01-01T00:00:00Z",closesAt:"2029-01-02T00:00:00Z",eligibleArtistType:"either",minimumFame:null,maximumFame:null,preferredGenres:[],stageTypes:[],canApply:true}],applications:[],invitations:[],offers:[],bookings:[],permissions:{profileId:id,canApplySolo:true,managedBandIds:[]}}).openApplications).toHaveLength(1));it("rejects cross-shaped candidate identities",()=>expect(()=>parseFestivalArtistCandidates({items:[{identity:{type:"solo",artistProfileId:id,bandId:id},displayName:"Bad",genres:[],fame:0,popularity:0}],limit:25,offset:0})).toThrow("malformed_festival_artist_programme_result"));it("requires the canonical action entity",()=>expect(()=>parseFestivalArtistActionResult({kind:"offer"})).toThrow("malformed_festival_artist_action_result"));});

it("preserves structured server eligibility reasons",()=>{const result=parseFestivalArtistOpportunities({openApplications:[{windowId:id,festivalCompanyId:id,name:"Open",opensAt:"2029-01-01T00:00:00Z",closesAt:"2029-01-02T00:00:00Z",eligibleArtistType:"band",minimumFame:100,maximumFame:null,preferredGenres:["rock"],stageTypes:[],canApply:false,eligibility:{eligible:false,reasons:[{code:"FESTIVAL_APPLICATION_INSUFFICIENT_SONGS",required:6,actual:4}]}}],applications:[],invitations:[],offers:[],bookings:[],permissions:{profileId:id,canApplySolo:false,managedBandIds:[id]}});expect(result.openApplications[0].eligibility?.reasons[0]).toEqual({code:"FESTIVAL_APPLICATION_INSUFFICIENT_SONGS",required:6,actual:4})});
