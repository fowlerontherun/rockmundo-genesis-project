import {describe,expect,it} from "vitest";
import {affectedFestivalDates,festivalDates,formatMinorMoney,parseFestivalTicketPlanResult,parseMoneyToMinor,type FestivalTicketProduct} from "../domain/festivalTicketPlan";
import {mulBasisPoints,previewTicketForecast} from "../domain/festivalTicketForecast";
const product:FestivalTicketProduct={id:null,name:"Full Festival",slug:"full",ticketType:"full_festival",productClass:"admission",accessScope:"full_festival",validFromDate:"2030-06-01",validToDate:"2030-06-03",priceMinor:12550,faceValueMinor:12550,capacityLimit:100,minimumAge:null,includesCamping:false,includesParking:false,includesVipArea:false,includesBackstage:false,transferable:true,refundable:false,salePriority:0,active:true};
describe("Festival ticket Phase 3 domain",()=>{it("parses and formats integer minor units safely",()=>{expect(parseMoneyToMinor("125.50")).toBe(12550);expect(parseMoneyToMinor("1.005")).toBeNull();expect(formatMinorMoney(4500,"GBP")).toContain("45.00")});it("expands full-festival admission but excludes add-ons",()=>{const dates=festivalDates("2030-06-01","2030-06-03");expect(affectedFestivalDates(product,dates)).toEqual(dates);expect(affectedFestivalDates({...product,productClass:"add_on",accessScope:"non_admission"},dates)).toEqual([])});it("uses deterministic fixed-point forecast arithmetic",()=>{expect(mulBasisPoints(12550,8000)).toBe(10040);const forecast=previewTicketForecast({ticketPlan:{id:null,currencyCode:"GBP",salesTaxRateBasisPoints:2000,bookingFeeMode:"fixed",bookingFeeMinor:250,bookingFeeBasisPoints:0,bookingFeePayer:"customer",refundPolicy:"",transferPolicy:"",minimumPurchaseQuantity:1,maximumPurchaseQuantity:8,expectedSellThroughBasisPoints:8000,expectedRefundBasisPoints:0,expectedComplimentaryUseBasisPoints:8000,expectedNoShowBasisPoints:500,status:"in_progress"},products:[product],releasePhases:[],capacityAllocations:[]},["2030-06-01"],100);expect(forecast.expectedGrossTicketReceiptsMinor).toBe(1004000);expect(forecast.expectedTicketsSold).toBe(80)});it("rejects a malformed whole RPC payload",()=>{expect(()=>parseFestivalTicketPlanResult({festivalCompanyId:"not-a-uuid"})).toThrow("malformed_festival_ticket_plan_result")})});
