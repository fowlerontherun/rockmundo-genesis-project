import type { DerivedPlaybackState } from "./engine/PlaybackController"; import { formatTime } from "./GigViewerTimeline";
export function GigViewerStatus({ state, attendance, capacity }: { state: DerivedPlaybackState; attendance?: number | null; capacity?: number | null }) { return <div className="rounded-lg border bg-muted/30 p-3" aria-live="polite"><p className="font-semibold">{state.activePhase.replace(/_/g, " ")}{state.currentSongTitle ? ` · ${state.currentSongTitle}` : ""}</p><p className="text-sm text-muted-foreground">{formatTime(state.positionMs)} / {formatTime(state.durationMs)} · Attendance {attendance ?? "unknown"}{capacity ? ` / ${capacity}` : ""}{state.crowdEnergy != null ? ` · Crowd energy ${Math.round(state.crowdEnergy)}` : ""}</p></div>; }
