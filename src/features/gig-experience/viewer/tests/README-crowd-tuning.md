# Crowd tuning test scope

The crowd tuning tests cover demo-only visual overrides. They verify that:

- production plans remain unchanged when no override is supplied;
- visual density changes do not alter authoritative attendance;
- stage and centre compression remain bounded inside audience zones;
- fan marker size and arrival timing can be adjusted independently;
- the controls only identify the admin gig viewer demo route.
