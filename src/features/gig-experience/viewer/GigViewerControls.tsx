import { Button } from "@/components/ui/button";
import { Switch } from "@/components/ui/switch";
import type { PlaybackSpeed } from "./engine/PlaybackController";
export function GigViewerControls({ playing, complete, speed, reducedMotion, onPlay, onPause, onRestart, onSpeed, onPrevious, onNext, onResult, onClose, onReducedMotion }: { playing: boolean; complete: boolean; speed: PlaybackSpeed; reducedMotion: boolean; onPlay: () => void; onPause: () => void; onRestart: () => void; onSpeed: (speed: PlaybackSpeed) => void; onPrevious: () => void; onNext: () => void; onResult: () => void; onClose: () => void; onReducedMotion: (v: boolean) => void }) { return <div className="flex flex-wrap items-center gap-2 rounded-lg border bg-card p-3" aria-label="Replay controls"><Button onClick={playing ? onPause : onPlay} aria-pressed={playing}>{playing ? "Pause" : complete ? "Play again" : "Play"}</Button><Button variant="outline" onClick={onRestart}>Restart</Button><Button variant="outline" onClick={onPrevious}>Previous event</Button><Button variant="outline" onClick={onNext}>Next event</Button><div className="flex rounded-md border p-1" role="group" aria-label="Playback speed">{([1,2,4] as PlaybackSpeed[]).map((s) => <Button key={s} size="sm" variant={speed === s ? "default" : "ghost"} aria-pressed={speed === s} onClick={() => onSpeed(s)}>{s === 4 ? "Fast" : `${s}×`}</Button>)}</div><label className="flex min-h-10 items-center gap-2 rounded-md border px-3 text-sm"><Switch checked={reducedMotion} onCheckedChange={onReducedMotion} aria-label="Reduced motion" />Reduced motion</label><Button variant="secondary" onClick={onResult}>View Result</Button><Button variant="ghost" onClick={onClose}>Close Viewer</Button></div>; }
