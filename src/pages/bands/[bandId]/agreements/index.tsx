import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Badge } from '@/components/ui/badge';
import { Link, useParams } from 'react-router-dom';

const sections = ['Active agreements','Awaiting signatures','Drafts','Expiring soon','Superseded agreements','Terminated agreements','Member agreements','Revenue-share agreements','Creative agreements'];
export default function BandAgreementsDashboard() { const { bandId } = useParams(); return <main className="mx-auto flex max-w-6xl flex-col gap-4 p-4 md:p-6"><div><h1 className="text-2xl font-bold">Band agreements</h1><p className="text-muted-foreground">Gameplay terms for membership, revenue shares, departure notice and creative-right protections.</p></div><div className="flex gap-2"><Link className="rounded-md bg-primary px-3 py-2 text-primary-foreground" to={`/bands/${bandId}/agreements/new`}>Create agreement</Link><Link className="rounded-md border px-3 py-2" to={`/bands/${bandId}/leave`}>Departure notice</Link></div><div className="grid gap-3 md:grid-cols-3">{sections.map((s,i)=><Card key={s}><CardHeader><CardTitle className="flex items-center justify-between text-base">{s}<Badge variant={i<2?'default':'secondary'}>0</Badge></CardTitle></CardHeader><CardContent><p className="text-sm text-muted-foreground">No {s.toLowerCase()} to show.</p></CardContent></Card>)}</div></main>; }
